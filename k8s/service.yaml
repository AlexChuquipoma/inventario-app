apiVersion: v1
kind: Service
metadata:
  name: inventario-app
  labels:
    app: inventario-app
spec:
  type: ClusterIP
  selector:
    app: inventario-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
