apiVersion: v1
kind: Service
metadata:
  name: inventario-app-bg
  labels:
    app: inventario-app
spec:
  type: ClusterIP
  selector:
    app: inventario-app
    slot: blue
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
